programa {
  funcao inicio() {
    cadeia velocidade

    escreva("Qual a velocidade do veículo? ")
    leia(velocidade)

    se(velocidade >= 80) {escreva("Multado. ")}
    senao{escreva("Dentro do limite. ")}
  }
}
